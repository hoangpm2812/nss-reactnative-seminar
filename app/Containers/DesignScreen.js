import React, { Component } from 'react'
import {
    SafeAreaView,
    StyleSheet,
    ScrollView,
    View,
    Text,
    Button,
    StatusBar,
} from 'react-native';
import styles from './Styles/DesignScreenStyles'

class DesignScreen extends Component {
    render() {
        return (
            <View style={{flex: 1}}>

            </View>
        )
    }
}

export default DesignScreen